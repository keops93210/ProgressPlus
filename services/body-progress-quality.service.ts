import type { BodyMeasurement } from "@/services/body-progress.service";
export type BodyQualityIssue='same_day_duplicate'|'identical_measurements'|'missing_core_measurement';
export function getBodyProgressQuality(measurements:BodyMeasurement[]){const sorted=[...measurements].sort((a,b)=>new Date(b.measured_at).getTime()-new Date(a.measured_at).getTime());const issues:BodyQualityIssue[]=[];const latest=sorted[0];const previous=sorted[1];if(latest&&previous){const a=new Date(latest.measured_at);const b=new Date(previous.measured_at);if(a.getFullYear()===b.getFullYear()&&a.getMonth()===b.getMonth()&&a.getDate()===b.getDate())issues.push('same_day_duplicate');const fields:(keyof BodyMeasurement)[]=['weight_kg','body_fat_percent','chest_cm','arm_cm','waist_cm','thigh_cm'];const comparable=fields.filter((f)=>typeof latest[f]==='number'&&typeof previous[f]==='number');if(comparable.length>=3&&comparable.every((f)=>latest[f]===previous[f]))issues.push('identical_measurements');}if(latest&&!latest.weight_kg&&!latest.waist_cm)issues.push('missing_core_measurement');return{valid:issues.length===0,issues};}
