export type ScorePoint={date:string;score:number};
export function getProgressScoreTrend(points:ScorePoint[]){const sorted=[...points].filter((p)=>Number.isFinite(p.score)).sort((a,b)=>new Date(a.date).getTime()-new Date(b.date).getTime());if(!sorted.length)return{current:null as number|null,delta:null as number|null,direction:'stable' as const,points:[] as ScorePoint[]};const current=sorted[sorted.length-1].score;const previous=sorted.length>1?sorted[sorted.length-2].score:null;const delta=previous==null?null:current-previous;return{current,delta,direction:delta==null||delta===0?'stable' as const:delta>0?'up' as const:'down' as const,points:sorted};}
