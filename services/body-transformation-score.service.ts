import type { BodyMeasurement } from "@/services/body-progress.service";
export type BodyTransformationTrend={score:number|null;label:string;weightDelta:number|null;waistDelta:number|null;message:string};
export function getBodyTransformationTrend(measurements:BodyMeasurement[]):BodyTransformationTrend{const sorted=[...measurements].sort((a,b)=>new Date(a.measured_at).getTime()-new Date(b.measured_at).getTime());if(sorted.length<2)return{score:null,label:'Données insuffisantes',weightDelta:null,waistDelta:null,message:'Ajoute au moins deux mesures pour suivre ta transformation.'};const first=sorted[0],last=sorted[sorted.length-1];const delta=(a:number|null,b:number|null)=>typeof a==='number'&&typeof b==='number'?Number((b-a).toFixed(1)):null;const weightDelta=delta(first.weight_kg,last.weight_kg);const waistDelta=delta(first.waist_cm,last.waist_cm);let score=50;if(waistDelta!=null)score+=Math.max(-25,Math.min(25,-waistDelta*5));if(weightDelta!=null&&weightDelta<0)score+=Math.min(15,Math.abs(weightDelta)*2);score=Math.max(0,Math.min(100,Math.round(score)));return{score,label:score>=75?'Transformation favorable':score>=55?'Transformation en cours':'Transformation à surveiller',weightDelta,waistDelta,message:score>=75?'La tendance corporelle est favorable. Continue à mesurer dans les mêmes conditions.':score>=55?'Ta transformation évolue : garde des mesures régulières pour confirmer la tendance.':'La tendance mérite d’être suivie sur plusieurs mesures avant de conclure.'};}
