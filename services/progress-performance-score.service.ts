export type PerformanceInput={currentE1rm:number|null;previousE1rm:number|null;volumeChangePercent:number|null;prCount:number;sessionCount:number};
export function getPerformanceScore(i:PerformanceInput){const signals:number[]=[];if(i.currentE1rm!=null&&i.previousE1rm!=null&&i.previousE1rm>0)signals.push(Math.max(0,Math.min(100,50+((i.currentE1rm-i.previousE1rm)/i.previousE1rm)*1000)));if(i.volumeChangePercent!=null)signals.push(Math.max(0,Math.min(100,50+i.volumeChangePercent*2)));if(i.prCount>0)signals.push(Math.min(100,60+i.prCount*10));if(i.sessionCount>0)signals.push(Math.min(100,50+Math.min(i.sessionCount,10)*5));if(!signals.length)return{score:null as number|null,label:'Pas assez de données',confidence:0};const score=Math.round(signals.reduce((a,b)=>a+b,0)/signals.length);return{score,label:score>=80?'Excellente performance':score>=65?'Bonne performance':score>=50?'Performance stable':'Performance à relancer',confidence:Math.round(signals.length/4*100)};}
