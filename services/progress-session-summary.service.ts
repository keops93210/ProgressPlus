export type SessionSummaryInput={exerciseCount:number;sets:number;volume:number;prs:number;durationMinutes:number;averageRir:number|null};
export function getSessionSummary(i:SessionSummaryInput){const highlights:string[]=[];if(i.prs>0)highlights.push(`${i.prs} nouveau${i.prs>1?'x':''} PR`);if(i.volume>0)highlights.push(`${Math.round(i.volume)} kg de volume`);if(i.exerciseCount>0)highlights.push(`${i.exerciseCount} exercice${i.exerciseCount>1?'s':''}`);const intensity=i.averageRir==null?'Non renseignée':i.averageRir<=1?'Très élevée':i.averageRir<=2?'Élevée':'Contrôlée';return{highlights,intensity,headline:i.prs>0?'Belle séance : nouveau record !':i.volume>0?'Séance enregistrée.':'Séance à compléter.'};}
