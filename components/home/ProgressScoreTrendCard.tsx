import { ArrowDownRight, ArrowUpRight, Minus, Sparkles } from "lucide-react-native";
import { StyleSheet, Text, View } from "react-native";
import Colors from "@/constants/colors";
import { getProgressScoreTrend, type ScorePoint } from "@/services/progress-score-trend.service";

type Props={points:ScorePoint[]};
export function ProgressScoreTrendCard({points}:Props){const trend=getProgressScoreTrend(points);const Icon=trend.direction==='up'?ArrowUpRight:trend.direction==='down'?ArrowDownRight:Minus;const tone=trend.direction==='up'?Colors.success:trend.direction==='down'?Colors.danger:Colors.primaryLight;return <View style={styles.card}><View style={styles.icon}><Sparkles size={18} color={Colors.primaryLight}/></View><View style={styles.body}><Text style={styles.eyebrow}>DYNAMIQUE DU SCORE</Text><Text style={styles.title}>{trend.current==null?'Pas encore assez de données':`Progress+ Score ${Math.round(trend.current)}`}</Text><View style={styles.row}>{trend.delta==null?<Text style={styles.meta}>Première mesure du score</Text>:<><Icon size={15} color={tone}/><Text style={[styles.delta,{color:tone}]}>{trend.delta>0?'+':''}{trend.delta} point{Math.abs(trend.delta)>1?'s':''}</Text><Text style={styles.meta}>depuis la dernière mesure</Text></>}</View></View></View>}
const styles=StyleSheet.create({card:{flexDirection:'row',alignItems:'center',backgroundColor:Colors.surface,borderRadius:18,borderWidth:1,borderColor:Colors.border,padding:14},icon:{width:38,height:38,borderRadius:12,backgroundColor:Colors.primarySoft,alignItems:'center',justifyContent:'center',marginRight:11},body:{flex:1},eyebrow:{color:Colors.primaryLight,fontSize:9,fontWeight:'900',letterSpacing:1.2},title:{color:Colors.text,fontSize:16,fontWeight:'900',marginTop:3},row:{flexDirection:'row',alignItems:'center',gap:5,marginTop:5},delta:{fontSize:11,fontWeight:'900'},meta:{color:Colors.textMuted,fontSize:10,fontWeight:'700'}});
